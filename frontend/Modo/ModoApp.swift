// App entry point; launches ContentView (which is the current main page).


import SwiftUI
import SwiftData

@main
struct ModoApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
